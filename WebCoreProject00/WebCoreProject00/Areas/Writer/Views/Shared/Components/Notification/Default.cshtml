@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
@using EntityLayer.Concrete
@model List<Announcements>
@{string color = ""; }
<div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="notificationDropdown">
    <p class="mb-0 font-weight-normal float-left dropdown-header">Bildirimler</p>
    @foreach (var n in Model)
    {
        if (n.Status == "Yeni Döküman")
        {
            color = "preview-icon bg-danger";
        }
        if (n.Status == "Yeni Video")
        {
            color = "preview-icon bg-warning";
        }
        if (n.Status == "Toplantı")
        {
            color = "preview-icon bg-info";
        }

        <a class="dropdown-item preview-item" href="/Writer/Default/AnnouncementsDetails/@n.ID">
            <div class="preview-thumbnail">
                <div class="@color">
                    <i class="ti-info-alt mx-0"></i>
                </div>
            </div>
            <div class="preview-item-content">
                <h6 class="preview-subject font-weight-normal">@n.Title</h6>
                <p class="font-weight-light small-text mb-0 text-muted">
                    @n.Status
                </p>
            </div>
        </a>
    }
</div>

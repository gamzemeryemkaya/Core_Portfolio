@model EntityLayer.Concrete.Skill
@{
    ViewData["Title"] = "GetSkill";
    Layout = "~/Views/Shared/_NewAdminLayout.cshtml";
}
@using (Html.BeginForm("EditSkill", "Skill", FormMethod.Post))
    {
        <br />
        <div class="col-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <p class="card-description">Yetenek Düzenleme Sayfası</p>
                    <form class="forms-sample">
                        <div class="form-group">
                            <label>ID</label>
                            @Html.TextBoxFor(x => x.SkillID, new { @class = "form-control" })
                        </div>
                        <div class="form-group">
                            <label>Yetenek Başlığı</label>
                            @Html.TextBoxFor(x => x.Title, new { @class = "form-control" })
                        </div>
                        <div class="form-group">
                            <label>Değer</label>
                            @Html.TextBoxFor(x => x.Value, new { @class = "form-control" })

                        </div>
                        <button type="submit" class="btn btn-primary mr-2" style="background-color:cadetblue">Güncelle</button>
                        <button class="btn btn-dark">İptal</button>
                    </form>
                </div>
            </div>
        </div>
    }

